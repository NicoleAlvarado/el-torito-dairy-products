---
import Layout from "@layouts/Layout.astro";
import PoliciesSection from "@sections/PoliciesSection.astro";
import Navbar from "@components/Navbar.astro";
import Footer from "@components/Footer.astro";

import { basics, termsAndConditions } from "@data";

const { name } = basics;
const { title, description, content } = termsAndConditions;
---

<Layout title=`Política de privacidad de ${name}`>
    <div>
        <header>
            <Navbar />
        </header>
        <main>
            <PoliciesSection title={title} description={description} content={content} />
        </main>
        <Footer />
    </div>
</Layout>

<style>
    div {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 3rem;
        position: relative;
        min-height: 100dvh;
    }

    div > header {
        padding: 1rem;
        width: 100%;
        display: flex;
        justify-content: center;
        background-color: var(--primary-700);
    }

    div > main {
        padding: 1rem;
        flex: 1;
        width: min(100%, calc(var(--max-width) / 1.5));
    }
</style>
