---
type Props = {
    id: "our-team" | "testimonies" | "products" | "about-us";
    flexDirection: "row" | "column";
};

const { id, flexDirection } = Astro.props;
---

<section id={id}>
    <slot />
</section>

<style define:vars={{ flexDirection }}>
    section {
        padding-block: 1rem;
        width: min(100%, var(--max-width));
        display: flex;
        flex-direction: var(--flexDirection);
        gap: 2rem;
        min-height: 100vh;
    }
</style>
